package main

import "fmt"

func main() {
	/**
	🔹 Built-in Scalar Types:
		bool

		Integer types: int, int8, int16, int32, int64

		Unsigned: uint, uint8 (alias byte), etc.

		Floating point: float32, float64

		complex64, complex128

		string

		rune (alias for int32, represents Unicode)

		🔹 Composite Types:
		array

		slice (more powerful than array)

		map

		struct

		🔹 Interface Types:
		interface{} — empty interface

		Custom interfaces

		🔹 Function types:
		Yes, func types exist and are first-class citizens

		🔹 User-defined types:
		struct, interface, type MyType int (custom alias)

		🔹 Pseudo-class types:
		No classes, but struct + methods = OOP-style

		No self, parent, or static — Go uses value receivers and interfaces

		🔹 Special types:
		nil (for pointer, map, slice, func, interface)

		No null, void, never, or resource types as in PHP or TypeScript

		No true/false types (those are values of bool, not types)

		No unit type — closest is struct{} (empty struct)
	*/

}
